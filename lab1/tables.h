#pragma once
#include <map>
#include <set>
#include <string>
#include <fstream>

using namespace std;



extern map<string, size_t> key_word_table;
extern map<string, size_t> identifier_table;
extern map<string, size_t> constants_table;
extern map<string, size_t> control_task_table;